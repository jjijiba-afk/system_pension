# 연금계리 산출 시스템

K-IFRS 1019호 '종업원급여' 에 따른 **확정급여채무(DBO) 산출 시스템** 입니다.

기존 엑셀 VBA 매크로(`sample_2512_..._vba_v3.2.xlsm`)가 하던 명부 정규화·검증을
그대로 이식하고, 그 위에 **예측단위적립방식(PUC) 계리 엔진** 을 얹었습니다.
클리닝된 명부와 기초율 엑셀을 넣으면 확정급여채무·근무원가·민감도·증감분석이
담긴 엑셀 리포트가 나옵니다.

---

## 무엇이 달라졌나

| | 기존 VBA | 이 시스템 |
|---|---|---|
| 날짜 파싱 | 같은 900줄 블록을 항목마다 복사 (모듈당 4~5회) | 함수 1개 (`dates.py`), 형식별 회귀 테스트 39건 |
| 오류 처리 | `MsgBox` 띄우고 첫 오류에서 산출 중단 | 전부 수집 → **시트·행·열·사번** 과 함께 리포트 |
| 명부 끝 판정 | `CountA(H:H)` 로 건수를 세어 그만큼만 읽음 → 중간에 빈 칸이 있으면 뒤쪽 인원 누락 | 실제 마지막 행을 탐색 |
| 메모리 | `Dim c_sabeon(1 To 100000)` 배열 40여 개 (모듈당 약 30MB) | 명부 크기에 비례 |
| 산출 범위 | 업로드 명부 생성까지 | + DBO·근무원가·이자원가·민감도·증감분석·장기급여 |
| 실행 | 엑셀 + 매크로 사용 허용 필요 | 단일 EXE (설치 불필요) |

기존 VBA에서 발견해 고친 버그와, 규칙이 서로 어긋나 **자동으로 고르지 않고 경고만
남긴** 항목은 [`docs/vba-mapping.md`](docs/vba-mapping.md) 에 전부 적어 두었습니다.

---

## 빠르게 써 보기

### 실행파일 (권장 — 파이썬 설치 불필요)

1. GitHub 저장소 → **Actions** 탭 → 최근 `Windows 실행파일 빌드` 실행 →
   **Artifacts** 의 `연금계리산출-windows` 다운로드
2. 압축을 풀고 `연금계리산출.exe` 더블클릭

### 소스에서 실행

```bash
pip install -e .

# GUI
python -m pension

# 배치
python -m pension assumptions --roster 명부.xlsm            # 산출 가정 입력 화면
python -m pension template 기초율.xlsx --roster 명부.xlsm   # 기초율 양식 생성
python -m pension check 명부.xlsm                            # 검증만
python -m pension calc 명부.xlsm 기초율.xlsx -o 산출결과.xlsx
python -m pension members 명부.xlsm 기초율.xlsx -o 개인별결과.xlsx  # 개인별 결과만
```

---

## 입력 파일 두 개

### 1. 명부 워크북

기존 VBA 통합문서를 그대로 씁니다. 시트 구조와 행 위치가 같아야 합니다.

| 시트 | 내용 | 데이터 시작 행 |
|---|---|---|
| `Input` | 산출기준일(C3), 평균임금 체크금액(C5), 직군 규칙(B12~N36) | 12 |
| `재직자명부` | 사번·직군·생년월일·입사일·평균임금·제도구분 등 36개 칼럼 | 26 |
| `퇴직자명부` | 퇴사일·지급사유·지급금액 등 24개 칼럼 | 22 |

`Input` 시트 G~N 열(기초율 규정명)을 채우면 **직군 단위로 일괄 적용** 되고,
비워 두면 명부의 해당 칼럼 값을 씁니다. VBA와 같은 규칙입니다.

### 2. 기초율 워크북

GUI의 **[산출 가정 입력]** 화면에서 채우거나, `pension template` 로 양식을 만들어
엑셀에서 채웁니다.

| 시트 | A열 | B열 이후 |
|---|---|---|
| `할인율` | 연차 | 할인율 — 한 줄이면 단일 할인율, 여러 줄이면 현물이자율 기간구조 |
| `임금상승률` | 연차 | Base-up 상승률 (승진·승급 제외) |
| `승급률` | 연령 또는 근속 | 규정명별 승급률 |
| `퇴직률` | 연령 또는 근속 | 규정명별 연간 중도퇴직률 |
| `사망률` | 연령 | 남자 / 여자 |
| `지급률` | 근속연수 | 규정명별 월평균임금 대비 배수 — **비우면 법정 퇴직금** |
| `지급률규정` | 규정명 | 방식(누적/누진/수식) · 수식 |
| `장기급여지급률` | 근속연수 | 규정명별 지급일수 (일 기본급 × 일수) |

지급률은 세 가지 방식으로 적을 수 있습니다 — **누적**(표 값이 누적 배수), **누진**
(표 값이 구간별 연 배수), **수식**(엑셀 문법의 조건부 계산식).
자세한 내용은 [지급률 규정 작성법](docs/지급률규정-작성법.md)을 보십시오.

모든 표는 **계단식 조회** 입니다. 20·25·30세 행만 있으면 27세는 25세 행 값을 씁니다.
A1 셀을 `연령` ↔ `근속` 으로 바꾸면 조회 기준이 바뀝니다.

---

## 결과 리포트

| 시트 | 내용 |
|---|---|
| `산출요약` | 기준·인원·DBO·근무원가·이자원가·듀레이션·직군별 내역 |
| `증감분석` | 기초 → 기말 Roll-forward, 보험수리적손익을 경험조정/가정변경으로 분해 |
| `민감도분석` | 할인율·임금상승률 ±0.5%p, 사망률·퇴직률 ±10% |
| `개인별산출` | 1인 1행. 검산과 원가배분용 |
| `장기급여` | 기타장기종업원급여 개인별 산출 |
| `검증리포트` | 명부 이슈 전체 (시트·행·열·사번 포함) |
| `UpLoad_Jae` / `UpLoad_Toi` | 정규화된 업로드 명부 (기존 VBA 출력과 동일한 열 구성) |

---

## 산출 방법

예측단위적립방식으로 재직자 한 명씩 정년까지 1년 단위로 투영합니다.

```
DBO = Σ_t  급여(t) × (과거근속 / 총근속(t)) × 탈퇴확률(t) × 현가계수(t)
```

* 임금은 Base-up + 승급률로 복리 투영
* 탈퇴는 중도퇴직률과 사망률을 함께 적용(`1 − (1−w)(1−q)`), 연중앙 발생 가정
* 정년 도달자는 마지막 시점에 전원 퇴직
* 당기근무원가는 귀속비율만 `1 / 총근속` 으로 바꾼 값

자세한 식과 근거는 [`docs/계리방법론.md`](docs/계리방법론.md) 에 있습니다.

**DC 가입자는 확정급여채무가 없습니다.** 부담금 납입으로 의무가 끝나기 때문이며,
결과의 `제외사유` 칼럼에 이유를 남깁니다.

---

## 개발

```bash
pip install -e ".[dev]"
python -m pytest          # 160건
python -m ruff check src/ tests/
```

```
src/pension/
  dates.py         명부 날짜 정규화 (VBA Select Case 이식)
  normalize.py     성별·임직원·제도·퇴직사유 코드값 정규화
  config.py        Input 시트 → 산출기준·직군 규칙
  readers.py       명부 읽기 (열 배치 표)
  validation.py    검증 (오류 일괄 수집)
  upload.py        UpLoad_Jae / UpLoad_Toi 생성
  assumptions.py   기초율 로딩 및 양식 생성
  members.py       개인별 결과 내보내기 (원가코드·직군별 집계)
  editor.py        산출 가정 입력 화면
  formula.py       지급률 규정 수식 (안전한 엑셀형 표현식)
  actuarial.py     연령·근속·정년연령
  valuation.py     PUC 확정급여채무
  longterm.py      기타장기종업원급여
  sensitivity.py   민감도분석
  rollforward.py   증감분석·보험수리적손익 분해
  pipeline.py      전 과정 실행
  report.py        엑셀 리포트
  gui.py / cli.py  사용자 인터페이스
```

---

## 면책

이 시스템은 산출 도구입니다. 기초율(할인율·퇴직률·사망률·승급률)의 적정성과
명부의 정확성은 사용자 책임이며, 재무제표 공시 전에는 계리사의 검토를 받으십시오.
`검증리포트` 시트에 남은 경고는 자동으로 판단할 수 없어 담당자 확인이 필요한
항목입니다.
